apiVersion: v1
kind: Service
metadata:
  name: {{ include "dhanya-lakshmi.fullname" . }}
  labels:
    {{- include "dhanya-lakshmi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dhanya-lakshmi.selectorLabels" . | nindent 4 }}
