apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "dhanya-lakshmi.fullname" . }}-config
  labels:
    {{- include "dhanya-lakshmi.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
